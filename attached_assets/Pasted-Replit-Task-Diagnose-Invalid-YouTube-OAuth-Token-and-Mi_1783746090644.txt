Replit Task: Diagnose Invalid YouTube OAuth Token and Missing Upload Scope

Current evidence:

The Connected Accounts page reports:

- Connected account found: Matthew Diaz
- Token invalid (400)
- error: invalid_token
- error_description: Invalid Value
- Upload scope: Missing
- Permission verified: Not yet

This indicates Google is rejecting the stored OAuth credentials.

Do NOT reconnect OAuth yet.

Determine the exact root cause first.

Tasks:

1. Inspect the stored Google OAuth credentials for the connected YouTube account.

2. Report whether the database contains:
   - access_token
   - refresh_token
   - expires_at
   - granted scopes

3. Determine whether the access token is:
   - expired
   - malformed
   - revoked
   - missing

4. Verify whether the refresh token exists.

5. If a refresh token exists:

   Attempt a token refresh using Google's OAuth endpoint.

6. Report:

   - success or failure
   - returned scopes
   - returned expiration
   - exact Google response

7. Verify which scopes were originally requested.

Specifically determine whether the OAuth flow includes:

https://www.googleapis.com/auth/youtube.upload

and

https://www.googleapis.com/auth/youtube

8. Inspect the OAuth authorization URL generated by AI Edge.

List every requested scope.

9. Inspect the token validation endpoint.

Verify it is validating the correct token.

10. Determine whether this is:

- expired access token
- failed refresh
- missing refresh token
- missing upload scope
- revoked consent
- incorrect OAuth configuration
- wrong Google account
- application bug

11. Do not reconnect OAuth unless the investigation proves that is the only possible fix.

Deliverables:

- Root cause
- Current token state
- Refresh token state
- Granted scopes
- OAuth scopes requested
- Google response
- Files inspected
- Whether code changes are required

Do not modify any code until the investigation is complete.
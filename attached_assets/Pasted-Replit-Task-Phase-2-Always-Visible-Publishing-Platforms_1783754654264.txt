Replit Task: Phase 2 — Always-Visible Publishing Platforms

Background:

Phase 1 created the canonical social platform registry in:

src/lib/social-providers.ts

It now contains:

- canonical provider IDs
- labels and descriptions
- icons and visual metadata
- normalized provider status
- capability flags
- helper functions

The registry currently covers:

- facebook
- instagram
- google_business
- youtube
- tiktok
- linkedin

Goal:

Update Publishing Center and Content Autopilot so every supported platform is always visible, even when it is:

- disconnected
- pending external approval
- coming soon
- not yet publish-capable

The interface must clearly distinguish platform availability and prevent unsupported actions.

Do not add LinkedIn backend publishing.
Do not bypass TikTok approval restrictions.
Do not modify OAuth flows.
Do not add drag-and-drop in this phase.

Before changing code:

1. Reconfirm the current registry fields and helpers in:

   src/lib/social-providers.ts

2. Reconfirm the actual backend publishing support for:

   - Facebook
   - Instagram
   - Google Business Profile
   - YouTube
   - TikTok
   - LinkedIn

3. Reconfirm the actual connection response shape from:

   GET /api/social-connections

4. Reconfirm the actual queue and publish payloads expected by:

   - Publishing Center
   - Content Autopilot
   - social-posts backend routes

5. Do not mark a platform usable merely because it exists in the registry.
   Runtime connection state and capability flags must both be considered.

PART A — Publishing Center

File likely involved:

src/pages/SocialPublishingPage.tsx

1. Replace remaining hardcoded active-platform and coming-soon lists with the canonical registry.

2. Render one platform control for every relevant provider:

   - Facebook
   - Instagram
   - Google Business Profile
   - YouTube
   - TikTok
   - LinkedIn

3. Do not include Apple Business as a social publishing destination unless actual publishing support exists.

4. Platform controls must display a clear state such as:

   - Connected
   - Ready
   - Disconnected
   - Pending Approval
   - Coming Soon
   - Unsupported

5. Behavior by state:

   Connected and publish-capable:
   - selectable
   - can be added to the queue
   - can use the existing publish workflow

   Disconnected but connect-capable:
   - visible but not selectable for publishing
   - show a Connect action linking to /admin/connections

   Pending approval:
   - visible but disabled
   - show a Pending Approval badge or message
   - do not attempt publishing

   Coming soon or unsupported:
   - visible but disabled
   - explain that publishing is not yet available

6. YouTube requirements:

   - YouTube must be clearly visible.
   - If connected and the registry marks queue/publish support true, it must be selectable.
   - Preserve the existing YouTube upload and publish workflow.
   - Do not upload a test video.
   - Do not require a real video upload during verification.

7. TikTok requirements:

   - TikTok must remain visible.
   - If the app is still pending TikTok approval, show Pending Approval.
   - Do not enable production publishing merely because a backend handler exists.
   - Preserve existing readiness and connection data.

8. LinkedIn requirements:

   - LinkedIn must be visible.
   - Because no backend publish handler currently exists, it must remain disabled.
   - Show Coming Soon or Publishing Not Yet Available.
   - Do not add a fake publish action.

9. Preserve existing post creation, editing, queueing, scheduling, and filtering behavior.

PART B — Content Autopilot

File likely involved:

src/pages/BBBContentAutopilotPage.tsx

1. Migrate the page from display-label platform identifiers to canonical SocialProviderId values where safe.

Current local display-label values may include:

- Facebook
- Instagram
- Google Business Profile

Replace internal platform identity with:

- facebook
- instagram
- google_business
- youtube
- tiktok
- linkedin

Use registry labels for display text.

2. Remove the duplicated local platform list and duplicated display metadata only after all current behavior is preserved.

3. Display all six platforms in the platform selector or content preview area.

4. Behavior by capability:

   generateText = true:
   - platform may display generated text content if returned by the backend

   queue = true and platform is operational:
   - Queue action enabled

   queue = false:
   - Queue action disabled with a clear explanation

   pending approval:
   - visible with Pending Approval state
   - no queue or publish action

   coming soon:
   - visible with Coming Soon state

5. YouTube:

   - Show YouTube in Content Autopilot.
   - Display YouTube-specific generated content only if it already exists in the API response, such as:
     - video title
     - description
     - Short script
     - caption
   - Do not invent content fields that the backend does not return.
   - If the content generator returns a YouTube Short Script under another key, normalize it safely in the frontend or report the backend contract mismatch.

6. TikTok:

   - Show TikTok in Content Autopilot.
   - Display existing TikTok script/caption content if already generated.
   - Keep queue/publish disabled while approval is pending.

7. LinkedIn:

   - Show LinkedIn-generated text if the backend already returns it.
   - Keep queue/publish disabled because the backend publish handler does not exist.

8. Preserve existing Facebook, Instagram, and Google Business Profile behavior exactly.

PART C — Auto Content Engine Alignment

Inspect:

src/pages/AutoContentEnginePage.tsx

Only make changes if needed to ensure its provider selectors also derive from the canonical registry.

Do not redesign the page.
Do not expose new platforms unless the page already has compatible generation behavior.

Report any remaining duplication rather than forcing unsafe changes.

PART D — Connection State

1. Use the existing social connection data.

2. Build a small reusable helper or hook only if one does not already exist.

3. The resolved platform state should consider:

   - provider capability flags
   - provider registry status
   - actual connection state
   - approval/readiness metadata
   - upload or publishing permission where applicable

4. Do not store connection state in the static registry.

The registry describes product capability.
The API response describes the user's live connection state.

5. Avoid duplicate requests to /social-connections.

If Publishing Center and its child components need the same data, share one TanStack Query cache key.

PART E — Reusable Platform Control

If Publishing Center and Content Autopilot currently duplicate the same platform button logic, create one small reusable component.

Suggested responsibilities:

- provider icon
- provider label
- state badge
- selected state
- disabled state
- tooltip or explanatory text
- optional Connect action

Do not turn this into a large generalized framework.

Keep page-specific actions outside the shared component.

PART F — Status Language

Use clear user-facing labels:

- Ready
- Connected
- Disconnected
- Pending Approval
- Coming Soon
- Publishing Unavailable

Avoid exposing raw internal status values such as:

- pending_approval
- not_configured
- unsupported

Status must be conveyed by text, not color alone.

PART G — Safety

1. Never enable an action when the backend capability is missing.

2. Never enable TikTok production publishing solely because a handler exists.

3. Never enable LinkedIn publishing.

4. Never expose access tokens, scopes, secrets, or raw provider metadata.

5. Do not change connection records or credentials.

6. Do not make any external publishing requests during browser verification.

PART H — Testing

Add focused tests for:

1. All six providers are visible in Publishing Center.

2. All six providers are visible in Content Autopilot.

3. Connected and publish-capable providers are selectable.

4. Disconnected providers show Connect and cannot publish.

5. TikTok shows Pending Approval and remains disabled when approval is incomplete.

6. LinkedIn shows Coming Soon or Publishing Unavailable and remains disabled.

7. YouTube becomes selectable when connected and publish-capable.

8. Existing Facebook, Instagram, and Google Business behavior is unchanged.

9. Platform labels and icons come from the canonical registry.

10. No duplicate local provider array remains in the two target pages.

PART I — Verification

Run:

- focused tests
- relevant frontend tests
- TypeScript typecheck
- production frontend build
- browser verification
- browser console inspection
- network inspection

Verify these pages:

- Publishing Center
- Content Autopilot
- Connected Accounts

Confirm:

- all supported platforms are visible
- YouTube is visible and usable when ready
- TikTok is visible but safely restricted
- LinkedIn is visible but safely disabled
- disconnected platforms link to Connected Accounts
- no unsupported publish requests are made
- no runtime errors
- no visual regression on mobile or desktop

Deliverables:

- Files modified
- Canonical registry fields reused
- Local platform definitions removed
- Remaining intentional local definitions
- Platform state-resolution logic
- Shared component created, if any
- Publishing Center before/after behavior
- Content Autopilot before/after behavior
- YouTube verification
- TikTok restriction verification
- LinkedIn restriction verification
- Tests added
- TypeScript result
- Build result
- Browser verification result
- Remaining backend gaps
- Checkpoint or commit created

Constraints:

- Do not add drag-and-drop.
- Do not add layout persistence.
- Do not add LinkedIn publishing.
- Do not bypass TikTok approval.
- Do not modify OAuth routes.
- Do not modify database schemas.
- Do not redesign unrelated pages.
- Do not use as any, @ts-ignore, or unsafe casts.
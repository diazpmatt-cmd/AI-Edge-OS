TARGET: Complete the Bed Bugs & Beyond YouTube pilot by attaching one real MP4 and performing one private upload through the normal production workflow.

IMPORTANT

Do not publish until Matthew has provided the actual MP4.

Do not:

- create a temporary upload bypass
- create another admin endpoint
- use a standalone publishing script
- change the approved title or description without approval
- upload to any channel other than:
  BedBugsand_Beyond
  Channel ID: UCGCZ49VYvCIff8rM-VU2eqA
- publish as public on the first attempt
- modify Google Business Profile work
- modify unrelated mobile code

CURRENT STAGED DRAFT

Draft ID:

34b0a41b-e08b-43b3-8167-c73655854ab5

Expected state:

- status: draft
- youtubeTitle: approved
- youtubeDescription/caption: approved
- youtubeTags: approved
- youtubePrivacy: private
- youtubeVideoId: null
- videoUrl: null
- publishedAt: null

PHASE 1 — SECURITY CLEANUP CHECK

Before upload:

1. Audit the scheduler-secret access added to the YouTube channel-info route.
2. Remove it if it was introduced only for this one-time staging audit.
3. Preserve Clerk authentication and the normal internal service-auth model.
4. Do not leave a broadly callable channel-info bypass.
5. Confirm no temporary stable scheduler secret was committed or exposed.
6. Confirm secrets remain environment-only.

Also identify the two earlier YouTube “Permission Test” uploads and report:

- provider video IDs
- privacy status
- whether they should be deleted manually in YouTube Studio
- do not delete them automatically without Matthew’s approval

PHASE 2 — MP4 ATTACHMENT

When Matthew provides the video file:

1. Use the existing reusable storage upload workflow:
   POST /storage/uploads/request-url

2. Validate before upload:
   - MIME type must be video/mp4
   - file must be non-empty
   - extension should be .mp4
   - recommended size under 100 MB for the current server flow
   - recommended codec: H.264 video with AAC audio

3. Upload the file to Replit Object Storage using the signed URL.

4. Save the resulting private object path to the staged draft’s videoUrl or existing media-reference field.

5. Do not make the object permanently public.

6. Confirm the API server can retrieve the object through the existing authenticated/private object route.

PHASE 3 — VIDEO VALIDATION

Before scheduling:

1. Confirm the stored object exists.
2. Confirm the server can read it.
3. Confirm content type is video/mp4.
4. Confirm file size.
5. If practical with existing tooling, confirm:
   - duration
   - width
   - height
   - orientation

6. Classify it truthfully:

   Short candidate:
   - vertical 9:16
   - appropriate Shorts duration under YouTube’s current eligibility rules

   Standard video:
   - landscape 16:9
   - or otherwise not Shorts-eligible

Do not claim Shorts classification solely from filename.

PHASE 4 — FINAL PRE-PUBLISH REVIEW

Return the following before triggering YouTube:

- channel name
- channel ID
- draft ID
- exact filename
- file size
- content type
- duration if known
- dimensions if known
- Short or standard classification
- exact title
- exact description
- exact tags
- privacy = private
- thumbnail behavior
- confirmation that no upload has occurred yet

Stop and require Matthew’s final confirmation for this exact file.

PHASE 5 — ONE PRIVATE UPLOAD

After Matthew’s final confirmation:

1. Use only the normal production draft/queue/publish workflow.
2. Set the staged draft to the correct queue or scheduled state.
3. Allow exactly one YouTube provider attempt.
4. Refresh the OAuth token normally if required.
5. Use the existing resumable-upload implementation.
6. Stop after the first provider failure.
7. Do not create a duplicate draft or second video upload.
8. Store:
   - youtubeVideoId
   - provider response summary
   - status
   - publishedAt
   - errorMessage if any
   - diagnostics

PHASE 6 — VERIFY

After a successful provider response:

1. Confirm the draft is marked published.
2. Confirm youtubeVideoId is stored.
3. Confirm publishedAt is stored.
4. Confirm privacy remains private.
5. Retrieve channel/video information through the existing authenticated YouTube API path.
6. Confirm:
   - video appears on BedBugsand_Beyond
   - title matches
   - description matches
   - privacy is private
7. Provide Matthew the YouTube Studio verification path.
8. Do not change privacy to public automatically.

PHASE 7 — FAILURE HANDLING

If the upload fails:

1. Stop after one attempt.
2. Preserve the staged draft and uploaded media.
3. Capture the sanitized YouTube error.
4. Classify the failure:
   - invalid token
   - missing scope
   - file retrieval failure
   - MIME/type rejection
   - file too large
   - resumable upload failure
   - metadata validation
   - quota
   - channel mismatch
5. Correct only the smallest necessary issue.
6. Do not create a second upload until the first failure is understood.

PHASE 8 — TESTS

Add or update tests for:

1. MP4-only validation.
2. Empty-file rejection.
3. Private object-path retrieval.
4. Draft cannot become publish-ready without videoUrl.
5. Channel ID must match UCGCZ49VYvCIff8rM-VU2eqA.
6. Privacy defaults to private.
7. One provider attempt only.
8. youtubeVideoId persistence.
9. Failed upload remains unpublished.
10. No duplicate upload.
11. Temporary channel-info bypass is absent.
12. Secrets are not committed.

PHASE 9 — REQUIRED REPORT

Return:

1. Security cleanup performed.
2. Status of the two permission-test videos.
3. File attached.
4. File validation results.
5. Draft ID.
6. Channel identity.
7. Final metadata.
8. Approval status.
9. Upload result.
10. youtubeVideoId if successful.
11. Final privacy state.
12. YouTube Studio verification status.
13. Files changed.
14. Tests and results.
15. Typecheck result.
16. Build result.
17. Remaining limitations.

DOCUMENTATION

Update:

- SESSION_HANDOFF.md
- CHANGELOG.md
- ROADMAP.md

Do not mark the YouTube pilot successful until the real MP4 is privately uploaded and verified on the BedBugsand_Beyond channel.
TARGET: Safely clean up the failed Google Business Profile pilot attempt and diagnose the exact 429/API-access blocker before any further publish retry.

IMPORTANT

The approved Google Business Profile post has NOT been published.

Stop all automatic and manual Google API retries until this task determines the exact blocker.

Do not:

- retry the publish endpoint
- clear cooldowns again
- bypass rate-limit protection
- guess a location resource name
- trust a browser dashboard URL as an API location ID without verification
- leave a temporary admin endpoint exposed
- expose tokens, secrets, or full provider responses containing credentials
- modify unrelated platforms or mobile code

PHASE 1 — AUDIT ALL CHANGES FROM THE FAILED ATTEMPT

Identify every file and dependency changed during the live-publish attempt, including:

- standalone TypeScript or JavaScript scripts
- package.json or lockfile changes
- temporary `tsx` installation
- one-time administrative endpoint
- social-posts route changes
- account-resource caching changes
- cooldown metadata changes
- database records created or modified
- draft, queue, publish-attempt, or diagnostic records

Return the exact diff and distinguish:

1. temporary/disposable changes
2. legitimate reusable production changes
3. database-only changes
4. uncommitted changes
5. already committed changes

PHASE 2 — REMOVE TEMPORARY AND UNSAFE MECHANISMS

Remove:

- the one-time administrative publishing endpoint
- disposable standalone publish scripts
- temporary debugging routes
- any temporary authentication bypass
- `tsx` or other dependency additions if they are not otherwise required by the project
- hard-coded account or location identifiers added only for this attempt

Do not remove a reusable caching improvement if it is correct, tested, and safe.

The production server must not retain an endpoint capable of publishing through an ad hoc secret or bypass path.

PHASE 3 — PRESERVE AND REVIEW LEGITIMATE CACHING

Audit the change that allows discovery APIs to be skipped when a verified account or location resource is cached.

Requirements:

1. Cached resource names must come from a successful Google API response or previously verified connection metadata.
2. They must be scoped to the correct tenant and social connection.
3. They must never be guessed from a browser URL.
4. The cached location must be verified using:
   - location title
   - account ownership
   - stored business identity
   - and, when available, phone, website, or address/service-area data
5. Invalid cached values must fail safely and trigger controlled rediscovery.
6. Do not call discovery APIs on every publish once a verified location is cached.
7. Add tests if this caching behavior remains.

PHASE 4 — CAPTURE THE EXACT GOOGLE ERROR

Inspect the latest safe server logs and normalized provider error.

Report, without exposing secrets:

- HTTP status
- Google error status
- Google error message
- reason/code fields
- service name
- quota metric if provided
- quota limit if provided
- retry-after header if present
- request endpoint category:
  - Account Management API
  - Business Information API
  - Local Posts API
- timestamps of attempts

Do not infer “daily quota” merely because the response remained 429.

Classify the blocker only if supported by the response:

- per-minute quota
- daily quota
- project quota set to zero
- Business Profile API access not approved
- API disabled
- wrong Google Cloud project
- invalid account access
- another documented Google error

If the response lacks sufficient detail, state that clearly.

PHASE 5 — AUDIT GOOGLE CLOUD/API CONFIGURATION

Determine from application configuration and available logs:

1. Which Google Cloud project owns the OAuth client.
2. Whether these services are enabled:
   - My Business Account Management API
   - My Business Business Information API
   - Google My Business API / local-post endpoint used by the application
3. Whether the project has approved Google Business Profile API access.
4. Whether quotas are nonzero.
5. Whether the OAuth client and enabled APIs belong to the same intended project.
6. Whether the connected token has:
   - business.manage
   - and any other required scopes
7. Whether the account is allowed to access the BB&B location.

If Google Cloud Console access is required from Matthew, provide exact navigation instructions and the exact values/screenshots needed. Do not claim those settings were verified if Replit cannot access the console.

PHASE 6 — COOLDOWN CORRECTION

Audit the current cooldown behavior.

Requirements:

1. Do not permanently persist a short provider cooldown without expiry handling.
2. Expired cooldowns must be ignored or automatically cleared.
3. Do not reset a long cooldown on every blind retry.
4. Honor Retry-After when Google provides it.
5. Store:
   - startedAt
   - expiresAt
   - reason
   - endpoint/service
   - attempt count
6. Prevent repeated retries while cooldown is active.
7. Separate temporary rate limiting from permanent API-access or zero-quota configuration errors.
8. Add tests for expiry and classification.

PHASE 7 — DATABASE STATE REVIEW

Inspect whether the failed attempt created:

- a draft
- queue records
- multiple publish attempts
- duplicate posts
- failed diagnostic records
- stale cooldown metadata
- partially seeded account metadata
- partially seeded location metadata

Normalize the state safely:

- preserve the approved draft
- ensure it remains unpublished
- mark failed attempts accurately
- remove duplicate temporary attempts only if safe and auditable
- do not fabricate a provider post ID
- do not mark anything published
- do not delete useful diagnostic evidence

PHASE 8 — DETERMINE THE ONE TRUE NEXT ACTION

At the end of the audit, select exactly one of these outcomes:

A. API access or nonzero quota must be requested/enabled in Google Cloud.
B. A specific quota must reset and the retry time is known from provider evidence.
C. A verified API location resource can be safely retrieved after one controlled call.
D. The OAuth connection must be reauthorized or repaired.
E. A different confirmed blocker exists.

Do not present multiple speculative options as equally likely.

PHASE 9 — PREPARE THE CONTROLLED RETRY PLAN

Do not execute the retry in this task.

Prepare a plan for one future attempt:

1. Confirm API access/quota.
2. Retrieve account/location only if not already verified.
3. Validate the location title is Bed Bugs & Beyond.
4. Cache the verified location resource name.
5. Use the already approved post.
6. Execute one normal production publish through the authenticated queue path.
7. Verify provider response and stored state.
8. Stop after one failure and report it.

PHASE 10 — TESTS AND CHECKS

Run:

- relevant provider/cooldown tests
- frontend tests if frontend code changed
- API-server typecheck
- frontend typecheck if applicable
- API-server build where supported
- frontend build if applicable

Add tests for:

- expired cooldown handling
- Retry-After handling
- zero-quota/access error classification
- verified location caching
- tenant-scoped cached location
- invalid cached location fallback
- no duplicate discovery calls when a verified location exists
- temporary admin endpoint no longer exists
- failed post remains unpublished

PHASE 11 — REQUIRED REPORT

Return:

1. Confirmation that no post was published.
2. Exact Google error classification with evidence.
3. Whether the problem is:
   - quota exhaustion
   - zero quota
   - missing API approval
   - disabled API
   - or something else
4. Exact service and endpoint failing.
5. Temporary files/routes/dependencies removed.
6. Legitimate code improvements preserved.
7. Current database state of the approved draft.
8. Current cooldown state.
9. Google Cloud Console action required from Matthew, if any.
10. Exact controlled retry plan.
11. Files changed.
12. Dependencies changed.
13. Tests and results.
14. Typecheck and build results.
15. Commit identifier if committed.
16. Remaining blocker.

DOCUMENTATION

Update:

- SESSION_HANDOFF.md
- CHANGELOG.md
- ROADMAP.md

Record that the first GBP pilot publish is blocked and has not succeeded.

Do not mark this as the first live BB&B publication.